#include <bits/stdc++.h>
using namespace std;
int main()
{
    int B, H;
    cout << "Enter the Base of Triangle (in mtrs) : ";
    cin >> B;
    cout << "Enter the Height of Triangle (in mtrs) : ";
    cin >> H;

    cout << "The Area of Triagle is " << (B * H) / 2 <<" mtr sq.";
}