#include <bits/stdc++.h>
using namespace std;
int main()
{
    float a, b;
    cout << "Enter 2 no. for Addition : ";
    cin >> a >> b;

    cout << "Add : " << a + b << endl;

    cout << "Enter 2 no. for Subtraction : ";
    cin >> a >> b;

    cout << "Sub : " << a - b << endl;
}